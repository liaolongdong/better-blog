source 'https://rubygems.org'

gem 'jekyll', '~> 4.3'
gem 'jekyll-paginate', '~> 1.1'

# Windows and JRuby does not include zoneinfo files so this bundle installs the zoneinfo gem
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem 'tzinfo', '~> 1.2'
  gem 'tzinfo-data'
end
